export default class Resources {

    constructor()
    {
        this.items = {}
    }
    /**
     This is an empty object to add the resources loaded by the Loaders.js javascript file.

     1. The Loaders.js file, load files based on the type of file (gltf, texture, images).
     2. And this object store the name of each file that need to be loaded for and easy access.
     */
}